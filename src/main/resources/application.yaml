logging:
  config: classpath:logback-spring.xml
  level:
    com:
      example:
        demo: DEBUG

management:
  server:
    port: 6150
  endpoints:
    web:
      exposure:
        include: health

server:
  port: 8010
  servlet:
    context-path: /notivent
    session:
      timeout: 15m
spring:
  datasource:
    jdbcurl: jdbc:postgresql://localhost:5432/notivent?currentSchema=mvp
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 05konrad05
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
  mail:
    host: smtp.mail.ru
    username: notivent@mail.ru
    password: TFzvVBDVs5ggbMg28mvU
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          port: 25
          auth: true
          starttls:
            enable: true
            required: true

mybatis:
  configuration:
    call-setters-on-nulls: false