<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.notivent.dao.UserDao">

    <select id="create" parameterType="map" resultType="ru.notivent.model.User">
        INSERT INTO mvp.user(user_name,password,created_at)
        VALUES(#{entity.userName},#{entity.password},now())
        RETURNING *
    </select>

    <select id="findById" resultType="ru.notivent.model.User">
        SELECT *
        FROM mvp.user
        WHERE uuid = #{uuid}::uuid
    </select>

    <select id="findByUserName" resultType="ru.notivent.model.User">
        SELECT *
        FROM mvp.user
        WHERE user_name = #{userName}
    </select>

    <update id="updateNickNameById">
        UPDATE mvp.user
        SET nick_name = #{NickName}
        WHERE uuid = #{uuid}::uuid
    </update>

</mapper>