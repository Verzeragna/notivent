<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.notivent.dao.CommentHistoryDao">

   <insert id="create">
       INSERT INTO geo_point_comments_history(user_uuid,geo_point_uuid,text)
       <foreach collection="comments" item="entity" separator=";">
           VALUES(#{entity.user.uuid},#{entity.geoPoint.uuid},#{entity.text})
       </foreach>
   </insert>
</mapper>