<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.notivent.dao.ContactsDAO">

    <select id="save" parameterType="map">
        INSERT INTO contacts(main_user_id,sub_user_id,created_at)
        VALUES(#{entity.mainUserId}::uuid,#{entity.subUserId}::uuid,#{entity.createdAt})
        ON CONFLICT DO NOTHING
    </select>

    <select id="findByUserId" resultType="ru.notivent.model.Contacts">
        SELECT *
        FROM contacts
        WHERE main_user_id = #{userId}::uuid OR sub_user_id = #{userId}::uuid
    </select>

    <select id="delete">
        DELETE FROM contacts
        WHERE (main_user_id = #{entity.mainUserId}::uuid AND sub_user_id = #{entity.subUserId}::uuid)
        OR main_user_id = #{entity.subUserId}::uuid AND sub_user_id = #{entity.mainUserId}::uuid
    </select>

    <select id="checkIfContactsExists" resultType="int">
        SELECT count(*)
        FROM contacts
        WHERE (main_user_id = #{entity.mainUserId}::uuid AND sub_user_id = #{entity.subUserId}::uuid)
        OR main_user_id = #{entity.subUserId}::uuid AND sub_user_id = #{entity.mainUserId}::uuid
        LIMIT 1
    </select>

</mapper>