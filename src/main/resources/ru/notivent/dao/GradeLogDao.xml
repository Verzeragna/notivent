<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.notivent.dao.GradeLogDao">

    <insert id="create" parameterType="map">
        INSERT INTO grade_log(user_uuid,grade_type,geo_point_uuid)
        VALUES(#{entity.userUuid}::uuid,#{entity.gradeType}::grade_type,#{entity.geoPointUuid})
    </insert>

    <select id="findByGeoPointAndUser" resultType="ru.notivent.model.GradeLog">
        SELECT *
        FROM grade_log
        WHERE geo_point_uuid = #{pointUuid}
        AND user_uuid = #{userUuid}::uuid
    </select>

    <update id="updateGradeType" parameterType="map">
        UPDATE grade_log
        SET grade_type = #{entity.gradeType}::grade_type,
            date = now()
        WHERE uuid = #{entity.uuid}
    </update>

    <delete id="delete" parameterType="map">
        DELETE FROM grade_log
        WHERE uuid = #{entity.uuid}::uuid
    </delete>

</mapper>